storage:
  dbPath: /data/db
  wiredTiger:
    engineConfig:
      cacheSizeGB: 0.25

net:
  port: 27017
  bindIpAll: true
  tls:
    mode: allowTLS
    certificateKeyFile: /etc/mongo/server-keycert.pem
    CAFile: /etc/mongo/ca.pem

security:
  authorization: enabled
